/**
 * Progress Tracking Routes (stub)
 */
import express from 'express';
const router = express.Router();

router.get('/', (req, res) => {
  res.json({ message: 'Progress tracking - not yet implemented' });
});

export default router;
