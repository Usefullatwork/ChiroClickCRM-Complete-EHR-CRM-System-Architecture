# ChiroClickCRM Frontend Development Dockerfile
# Optimized for hot-reload development with Vite

FROM node:20-alpine

WORKDIR /app

# Install dependencies (will be overridden by volume, but kept for standalone)
COPY package*.json ./
RUN npm ci

# Copy all source (for standalone mode)
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server with host binding for Docker
CMD ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0"]
